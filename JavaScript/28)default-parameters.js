function calInterest(rate, year = 33) {
  //   rate = rate || 3.5; //set default value //this is one way
  console.log(rate * year);
}

//when we use a default value for the parameter all the parameter values after that
//should be given a default value

calInterest(23, 34);
