//in js we have two kinds of strings.

//string primitive
const str = "hi";

//string object
const str2 = new String("hi All!!");

//slice() extracts a section of the string, and returns it as a new string,without modifying the
//original string.

let name = "Bro code";
let fname = name.slice(4);
